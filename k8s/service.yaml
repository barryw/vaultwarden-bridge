apiVersion: v1
kind: Service
metadata:
  name: vaultwarden-bridge
  namespace: vaultwarden-bridge
spec:
  selector:
    app: vaultwarden-bridge
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
